# 内音 (Naion)

AI Agent 기반 감정 언어화 코칭 앱.

## 시작하기

### Backend
```bash
cd backend
pip install -r requirements.txt
cp .env.example .env  # API 키 입력
uvicorn main:app --reload
```

### Frontend
```bash
cd frontend
npm install
npm run dev
```
